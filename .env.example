# HRMS Frontend - Environment Variables Example
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local or any file with actual secrets!

# Backend API Configuration
NEXT_PUBLIC_BACKEND_API_BASE_URL=http://localhost:8080
BACKEND_API_BASE_URL=http://localhost:8080

# JWT Configuration (Server-side only)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Token Storage Type: 'sessionStorage', 'localStorage', or 'cookie'
NEXT_PUBLIC_TOKEN_STORAGE_TYPE=sessionStorage

# Node Environment
NODE_ENV=development
